import {StyleSheet} from 'react-native';

export const CartListCSS = StyleSheet.create({
  cartList: {
    display: 'flex',
    gap: 5,
    marginBottom: 20,
  },
  totalPayment: {
    marginBottom: 20,
    textAlign: 'center',
  },
  buttonText: {
    fontSize: 16,
    color: 'white',
  },
});
