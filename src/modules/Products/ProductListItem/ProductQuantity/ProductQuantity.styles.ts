import {StyleSheet} from 'react-native';

export const productQuantityCSS = StyleSheet.create({
  wrapper: {
    flex: 1,
    display: 'flex',
    flexDirection: 'row',
    gap: 5,
  },
  quantityText: {
    fontSize: 16,
  },
});
